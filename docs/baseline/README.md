# Baseline capture

These screenshots were captured from
`../arcademic-nuxt/.output/public/index.html` via a local static server on
2026-08-29. See `../source-audit.md` for the source-state caveat and viewport
details.

The images are evidence only. Automated visual tests will generate fresh,
section-activated snapshots from controlled source and Rust builds.

